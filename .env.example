# Copie para .env no deploy:  cp .env.example .env  e edite o domínio.
# Domínio (subdomínio) com registro A apontando pro IP da VPS.
# O Caddy obtém e renova o HTTPS automaticamente para ele.
DOMAIN=pokerdex.seudominio.com
